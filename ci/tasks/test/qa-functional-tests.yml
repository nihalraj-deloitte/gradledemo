---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: dcil-docker-release.art.tools.deloitteinnovation.us/devi/command-line-utils
    tag: 1.7.2

run:
  user: root
  path: common-ci/tasks/utils/run-script.sh
  args:
    - common-ci/tasks/utils/dummy-test.sh
    - qa-functional-tests.log

params:
  # Left blank because we expect the pipeline to provide these values
  LOG_FOLDER:

inputs:
  - name: source-code
  - name: common-ci
  - name: input-artifacts

outputs:
  - name: output-artifacts
